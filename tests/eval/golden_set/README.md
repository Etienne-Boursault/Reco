# Golden set d'évaluation — fixtures synthétiques

Ces fichiers servent à TDD le harness `tools/eval`. Ils sont **synthétiques** (pas extraits d'un vrai épisode) — leurs `episode_guid` n'existent pas dans le dataset prod.

Voir `docs/adr/0011-eval-harness.md` pour le schéma complet et la procédure d'annotation pour les vrais épisodes (item complémentaire P1.4.B).
